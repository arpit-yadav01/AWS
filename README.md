# AWS S3 Image Upload Backend

Node.js + Express API to upload images to AWS S3.

## Features
- Upload image to S3
- Returns public URL
- Uses Multer
- AWS SDK v3

## Setup

npm install

Create .env:

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_BUCKET_NAME=

npm start
